# Web application

Planned stack: React + TypeScript + Redux Toolkit. Implement the responsive checkout UI here after the I0 contract and lifecycle gate. The browser may retain only the minimum checkout identifier and non-sensitive draft data needed for recovery; payment state must be fetched from the API. Never persist card data or provider secrets in the browser.
