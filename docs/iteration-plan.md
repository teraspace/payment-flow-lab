# Proposed iteration sequence

Estimate: one discovery/decision iteration (I0), six implementation iterations (I1–I6), and one final acceptance/release iteration (I7). This is a planning hypothesis; the real count is whatever the evidence ledger records. Each iteration is a coherent cycle with a frozen scope, acceptance criteria, checks, evidence, and a user validation gate before the next iteration starts.

| Iteration | Scope | Gate |
|---|---|---|
| I0 — Requirements and lifecycle | Trace PDF requirements/rubric to acceptance criteria; define initial API contract, DBMS-vs-domain idempotency, payment/reservation/fulfillment lifecycles, recovery and security boundaries; review the public-safe tree/history | User validates scope and provisional policies; public remote/first push and `main` protection are completed before I1 |
| I1 — Foundation | Initialize app workspaces, local PostgreSQL workflow, migrations, typed config, CI, and API/UI shell | Clean install, lint/typecheck/build, database migration, contract skeleton, and initial required CI checks evidence |
| I2 — Checkout and inventory | Catalog/checkout flow, server-side price calculation, atomic reservation, scoped idempotency, API persistence | PostgreSQL concurrency and duplicate-command gates pass |
| I3 — Payment lifecycle | Provider adapter, durable attempt/event records, webhooks, status lookup, reconciliation, timeout and retry policy | Controlled PostgreSQL/provider-contract evidence; no blind duplicate request under ambiguous outcome; user validates provisional retention/escalation defaults |
| I4 — React experience | Responsive checkout, accessible state/error handling, API integration, refresh recovery, approval/decline/pending/unknown states | Browser flow evidence on target mobile and desktop sizes |
| I5 — Reliability and scorecard | Boundary and concurrency tests, coverage threshold, security/error review, accessibility, performance and cross-browser fixes | Measured scorecard with defects resolved or clearly disclosed |
| I6 — Terraform and AWS | Cost-aware Terraform for a single Fargate service and PostgreSQL; configure secrets safely; deploy and smoke-test | Review plan/cost before apply; live endpoint and browser-to-API smoke evidence |
| I7 — Final acceptance and release | Re-run rubric, verify README/setup/evidence, inspect the public history/artifacts, and confirm deployed links | User validates final evidence and handoff; the repository is already public from I0 and has incremental feature history |
| I9 — Railway-oriented use-case errors | Add typed `Result` composition to checkout creation and payment-attempt creation; rollback transactions on expected `Err`; translate typed errors at the Nest HTTP boundary | Existing API contracts and idempotency behavior remain stable; no provider call moves into SQL; review the feature PR before merge |

I0 outputs are `requirements-traceability.md`, `api-contract.md`, and `payment-lifecycle.md`, together with the approval record in `open-decisions.md`. The user approved the scope and policies on 2026-09-24; the neutral public repository and `main` protection are in place; the I0 feature PR was merged as `c8d3fe0`. I0 is complete. I1 implementation and automated gates were completed on `feat/project-foundation` in PR #3 and merged into `main` on 2026-09-24; local and hosted checks passed. The user then requested an early deployment of the I1 shell; deployment PRs #4–#7 were approved and merged, and the live URL still serves that I1 foundation. This did not implement checkout or payment behavior.

I2 is merged as PR #8 (`5626a44`) and deployed to `sa-east-1`. It adds the API and PostgreSQL catalog/checkout/inventory slice; the React checkout UI remains in I4. The user approved the demo charges, 30-day guest-session lifetime, and 30-day PII retention from checkout creation. Local and hosted checks passed; the migration task exited `0`; ECS stabilized at 1/1; public API smoke checks passed; and the final Terraform plan reports no changes. The deployment URL and detailed evidence are in the README and `requirements-traceability.md`.

I3 merged as PR #10 (`23f9aa2`) and was deployed to Fargate. The migration task and API task stabilized; public liveness, readiness, and OpenAPI checks returned `200`. No real card or charge was used.

I4 merged as PR #12 (`0e047f7`). It adds the React catalog, delivery form, server-priced order summary, sandbox-only card tokenization, payment state/recovery, and same-origin API relays for acceptance metadata and browser-encrypted card tokenization. The sandbox smoke covered approval, decline, and an ambiguous retry that remains held as `UNKNOWN_OUTCOME`; no real card or charge was used. The customer reviewed and approved the PR before merge.

The AWS work planned as I6 was completed after I4. PR #13 merged the scoped ECS sandbox-secret injection. The user approved the deployment sequence; the CloudFront site is live and on 2026-09-25 returned `200` for the app, API liveness/readiness (`database: ok`), catalog, and API docs. The original planned order in the table remains a plan; actual delivery completed I6 before I5.

I5 merged as PR #14 (`a13eabf`) after user review; local gates and hosted `quality`, `database-migrations`, and `infrastructure` checks passed. On 2026-09-25 its frontend assets were deployed to the existing private S3 origin in `sa-east-1` using a reviewed plan that changed no ECS, RDS, IAM, secrets, network, or CloudFront distribution resources. Post-deploy smoke checks returned `200` for the app, JS/CSS bundles, API liveness/readiness, catalog, and docs; a subsequent Terraform plan reported no changes. Coverage, accessibility, browser, and local Lighthouse evidence are recorded in `reliability-scorecard.md`. The payment-module branch-coverage exception and Lighthouse device/deployment limits remain disclosed there.

I7's 2026-09-25 acceptance audit examined integrated `main` commit `a1a29d8` (PR #16), which included the prior I7 evidence. The audit found seven mandatory implementation/documentation failures and four unverified items; therefore I7 did **not** establish acceptance. Read-only checks on the pre-I8 deployment and previously passing CI do not prove the current checkout flow. No provider payment attempt or infrastructure apply was made during I7. The audit and its evidence are in `i7-final-acceptance.md`.

I8 (`codex/i8-checkout-acceptance`) remediates the audited modal/card/delivery flow, summary backdrop, five-stage progress, refresh-safe contact/delivery draft, pre-dispatch `PENDING` payment attempt, post-result catalog return, and README coverage disclosure. Local browser inspection at portrait 375 × 667 and landscape 667 × 375 CSS pixels found no horizontal overflow; the exact physical device/DPR remains unverified. On 2026-09-25 the web suite passed 8 suites / 76 tests at 89.19% statements, 83.56% branches, 86.76% functions, and 90.34% lines. The API suite passed 10 / 88 at 89.70%, 81.85%, 94.28%, and 91.03%; its migration was applied and rolled back against local test PostgreSQL. The payment module remains at 79.31% branch coverage, including 74.89% in `payments.service.ts`. The I8 PR, CI, deployment, and live sandbox transaction remain gates. The user must operate the final sandbox payment action; candidate sharing and the official 100-point score cannot be verified from code. See `i7-final-acceptance.md` for exact limits.

I9 applies typed `Result` composition to the two write use cases that establish checkout idempotency/inventory reservation and payment-attempt idempotency/dispatch. Its ROP error channel rolls back local writes on expected business failures; infrastructure failures remain exceptions. This is an application-flow pattern only and does not change PostgreSQL's transactional guarantees or make a provider request exactly once. PR #20 was reviewed by the user, merged as `dc226ce`, and deployed to ECS as image `i9-dc226ce` on task-definition revision 6. ECS stabilized at 1/1; public app, liveness, readiness, catalog, and docs checks returned HTTP 200. No migration or sandbox payment was run. The release evidence is in the I9 row of `ai-sdlc-ledger.md`.

Within an iteration, independent analyses can run in parallel when file ownership and contracts are clear. The review artifact is feature-based: normally one integrated PR for a vertical slice, or separate PRs only for independently reviewable features. A PR, commit, agent task, or prompt is not itself an iteration. Publish meaningful branch progress after the public-remote gate, then present the completed PR evidence to the user. Integration, user validation, merge, and the next iteration remain sequential. See `git-workflow.md` for the branch, commit, and PR conventions.
