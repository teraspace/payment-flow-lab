# AI-assisted SDLC experiment ledger

One row represents one integrated development cycle, not a prompt, agent message, or isolated subtask. The repository bootstrap below is a setup event before I0 and is excluded from the iteration count. Record tool/model/agent details only when actually observable. Distinguish AI work from human decisions and approvals.

| Iteration | Scope | Feature branch / PR / commits | Agents and tools | Human interventions | Elapsed / active time | Gates and evidence | Defects / rework | Outcome / next gate |
|---|---|---|---|---|---|---|---|
| Bootstrap (pre-I0) | Neutral scaffold and PDF-based feature/commit/PR workflow; no application code | `main` commit `4d790fb`; process commits `0c8011e` and `dc2f826` landed with I0 | Codex app; earlier PDF review; no new specialist agents in I0 | User requested analysis of incremental PRs, commit expectations, and completion of I0 | Not measured | Earlier PDF pages 1–7 reviewed with shared sandbox credential page excluded from notes; no tests or deployment run | I0 expands lifecycle details because failed/ambiguous payment semantics need an explicit gate | Process docs included in the public I0 feature PR |
| I0 (complete) | Trace requirements/rubric, define initial API contract and DBMS/domain idempotency boundary, specify payment failure/recovery, reservation and fulfillment lifecycle, and approve initial policies | Public feature branch `docs/iteration-0-baseline`; PR #1 merged on 2026-09-24 as `c8d3fe0`; closure evidence recorded in PR #2 | Codex app; official provider docs consulted for tokenization, acceptance, transaction, and event behavior; no specialist agents spawned | User approved the scope, lifecycle policies, public publication, and `main` protection on 2026-09-24 | Not measured | Requirements traceability, initial API contract, payment lifecycle, and approval record merged. `git diff --check` clean; internal Markdown links resolve; working-tree and commit-history prohibited-name/credential scans had no findings. No application tests, CI, or deployment run | Provider dispatch remains synchronous after local commit to avoid persisting card tokens; a future async design needs explicit secure token-retention handling | I0 complete: public repository and branch protection verified; I1 implementation is available for user review |
| I1 (complete) | Build the React/NestJS monorepo foundation, typed config, PostgreSQL migration workflow, health/OpenAPI shell, and required CI checks | `feat/project-foundation`; PR #3 merged on 2026-09-24 | Codex app; npm, Nest CLI, TypeScript, ESLint, Vite, PostgreSQL CLI, GitHub Actions, and agent-browser; no specialist subagents | User authorized I1 and installed/started Colima plus the Compose plugin, then validated and approved PR #3 | Not measured | Clean `npm ci`; `npm audit --audit-level=moderate` found 0 vulnerabilities; lint, typecheck, build passed; migration up/down and six product constraints checked against isolated PostgreSQL 17.6; after Colima setup, Compose started `postgres:17-alpine` with PostgreSQL 17.11 healthy and the migration applied; API liveness/readiness/OpenAPI returned HTTP 200 with DB ready; web rendered with no console errors/overlay or horizontal overflow at 390 px; hosted `quality` and `database-migrations` checks passed; `main` requires both checks. No automated tests were added or run | Fixed Nest output cache/`dist` mismatch and imported `DatabaseModule` explicitly; updated Nest schematics to clear five transitive audit findings; local Compose gap closed after Colima became available | I1 complete; I2 remains gated on user validation of the next iteration |
| I1 AWS deployment (complete) | Deploy the React/NestJS foundation shell to AWS: private S3/CloudFront UI, API on Fargate, private PostgreSQL, and a one-off migration. Checkout and payments are excluded | PR #4 merged as `0a82f77`; TLS correction PR #5 merged as `6999409`; steady-state correction PR #6 merged as `0f436bf` | Codex app; AWS CLI profile `payment-flow-lab`, Terraform 1.5.7, Colima/Docker, npm; no specialist agents | User approved PR #4/deployment, then chose the default CloudFront hostname and its effective TLSv1 policy | Not measured | Bootstrap preview was 44 creates, 0 changes, 0 destroys. After a temporary credential expiry during CloudFront propagation, Terraform state was recovered and the tainted distribution reconciled without replacement; the remaining bootstrap plan was 3 creates, 0 changes, 0 destroys. The API image was pushed to ECR; the one-off migration exited 0. Final service plan changed ECS desired count from 0 to 1 (0 creates, 1 change, 0 destroys). ECS stabilized at 1/1 and its ALB target is healthy; RDS is `available`, 20 GB, and not public; CloudFront is `Deployed` on its default hostname with effective TLSv1. HTTPS smoke checks returned 200 for `/`, `/api/v1/health/live`, `/api/v1/health/ready` (database `ok`), and `/api/v1/docs`. A final Terraform plan on `main` reports no changes with the deployed image and one-task default. PRs #4, #5, and #6 passed `quality`, `database-migrations`, and `infrastructure`; `main` requires all three. No application test suite was run; actual AWS charges have not yet been reviewed | Fixed the default CloudFront TLS mismatch and changed the desired-count default to match the deployed steady state. A short-lived AWS token expired during a long CloudFront wait; recovered state and completed with fresh per-process credentials | I1 deployment complete; I2 remains gated on user validation |
| I2 (complete and deployed) | Add PostgreSQL-backed catalog reads, guest session ownership, checkout snapshots, atomic inventory reservations, expiry, scoped idempotency, approved demo charges, and 30-day PII redaction. No payment provider, payment attempt, or React checkout UI | `feat/checkout-reservation`; PR #8 merged as `5626a44`; implementation commit `322a45c` | Codex app; earlier read-only architecture/QA reviews; npm, Jest, NestJS, TypeScript, ESLint, PostgreSQL 17 via Colima/Docker, Terraform 1.5.7, Docker ARM64, AWS CLI | User approved COP 5,000 base, COP 8,000 delivery, 30-day guest-session lifetime, and 30-day PII retention measured from checkout creation | Not measured | Local audit (0 advisories), lint, typecheck, build, Terraform fmt/validate passed; 14 Jest tests passed against isolated PostgreSQL and all migrations applied/rolled back; API coverage 91.40% lines, 63.51% branches, 90.18% statements, 85.18% functions. Hosted `quality`, `database-migrations`, and `infrastructure` passed on `ffbdc0f`. Image `i2-c7ab0c4` (linux/arm64, 90,439,844 bytes) pushed to private ECR. Migration-stage apply: 4 adds, 0 changes, 1 destroy (migration task-definition revision and three catalog SVGs; no API service update); migration task exited `0`. Final API apply: 1 add, 1 change, 1 destroy (API task definition and ECS service); ECS stabilized 1/1. Public smoke checks returned 200 for app, health, readiness, catalog, and docs; checkout create/replay/recovery returned 201/200/200 with total 42,000. Synthetic smoke data was deleted and inventory returned to 7 available/0 reserved. Final Terraform plan reports no changes. RDS is available, private, Single-AZ, 20 GB. Cost Explorer is unavailable to the CLI identity, so actual charges and credits remain unverified; the documented baseline is an estimate before variable usage | Expanded the I2 retention choice to erase the PII-derived idempotency fingerprint, return `410 Gone` for replay after redaction, and guard migration rollback from fabricating erased fields. First typecheck surfaced stale non-null Swagger response types; aligned contract and OpenAPI types, then all local gates passed | I2 complete and deployed. I3 payment lifecycle and I4 React checkout UI remain future iterations; coverage improvement remains in I5 |
| I3 (complete and deployed) | Add provider adapter, durable payment attempts and event receipts, signed webhooks, reconciliation, unknown-outcome handling, one bounded retry, atomic stock commitment/release, and one fulfillment per approval. No React UI | `feat/payment-lifecycle`; PR #10 merged as `23f9aa2`; final policy record `4b19db7` | Codex app; NestJS/TypeScript, Jest, PostgreSQL 17 via Colima/Docker, Terraform 1.5.7, AWS CLI/Docker ARM64; two independent read-only specialist reviews (backend lifecycle and QA/concurrency) | User initiated I3, emphasized recovery/idempotency and DBMS/domain consistency, and approved merge/deploy plus the 1,800-second review threshold and 365-day minimal receipt retention on 2026-09-24. | Not measured | `npm audit --audit-level=moderate` found 0 vulnerabilities; lint, typecheck, full build, Terraform fmt/init/validate passed; 41 Jest tests passed in five suites against real PostgreSQL; migrations applied and rolled back; `git diff --check` clean. No live provider credentials or real charge. Hosted `quality`, `database-migrations`, and `infrastructure` passed on PR #10 head `4b19db7`; public-safe scan found no prohibited provider name or credential matches; local `.env` is ignored. Image `i3-23f9aa2` (`linux/arm64`, digest `sha256:9fb84eb695ba1abee131e76f2b6e1fc5d897f725e8d7b7b04156a5a53e24890c`) was deployed after migration task exit `0`; ECS stabilized at 1/1 on task-definition revision 3; liveness/readiness/docs returned 200 and OpenAPI exposed ten API paths. | Initial tests exposed fixture assumptions about provider IDs/event time. Reviews found and fixes addressed inverted expiry/payment lock ordering, unsigned payment-authorizing event fields, late approval after retry deadline, HTTP 400/401 rejection consuming the charge retry, and contradictory status after approval. Added regression coverage; both reviewers rechecked the changed lifecycle, and QA recommended covering omission of every required signed field, which was added. The first unscoped Terraform plan also included unrelated S3 web-asset drift, so I3 applied only its reviewed migration/API ECS resources; the app assets, database, network, load balancer, and CDN were unchanged. | I3 implementation is merged and deployed. The React checkout remains I4; reconcile the unrelated generated web-asset drift in that iteration. |

| I4 (complete, merged, and deployed) | Build the React purchase flow, integrate checkout and latest-attempt recovery, show server-calculated totals, relay browser-encrypted sandbox card payloads, and present/poll payment states without duplicating unknown outcomes | `feat/react-checkout`; implementation commit `35fa67b`; merge commit `0e047f7`; PR #12 merged | Codex desktop; npm, ESLint, TypeScript, Nest build, Vite, CUA browser, PostgreSQL, official provider documentation; no specialist agents delegated in this turn | User authorized I4 and explicitly prohibited real charges and real-card data | Not measured | Local lint, typecheck, build, `git diff --check`, and 49 API tests across five suites passed. Browser/PostgreSQL smoke: declined attempt reconciled to `DECLINED`; one approved-card retry on that checkout became `UNKNOWN_OUTCOME` without provider ID, and stock remains held with no third attempt; a separate approved checkout reached `PAID`, committed stock, and created `READY` fulfillment. The UAT provider blocks browser CORS preflight, so the API fetches acceptance documents and the public encryption key and relays only the browser-generated JWE. Both consent controls were accepted; customer and delivery values were synthetic. No real card or real charge was used. PR CI and merge-commit CI (`quality`, `database-migrations`, and `infrastructure`) passed. AWS deployment followed in I6 after scoped sandbox-secret injection. | The first tokenization attempt failed at browser CORS before token/payment dispatch. Added same-origin API routes for the key and encrypted-card relay; DTO rejects plaintext card-shaped data. A retry timeout left no provider transaction ID, so it remains unknown and held until a verified event or manual review; no blind resend. Separate approved checkout verified the happy path. | I4 is merged; its AWS deployment and public smoke evidence are recorded in I6 below. |
| I6 (complete; executed after I4) | Inject only sandbox credentials into the ECS API task with scoped Secrets Manager access, then deploy the merged checkout and verify the public application/API | `fix/ecs-sandbox-secret-injection`; PR #13 merged as `a6393e4` | Codex desktop; Terraform, AWS CLI, ECS/Fargate, ECR, CloudFront, read-only HTTPS smoke checks | User authenticated AWS CLI and approved the deployment sequence on 2026-09-25 | Not measured | After user approval, the deployed CloudFront app, API liveness/readiness (`database: ok`), catalog, and API docs returned HTTP 200 on 2026-09-25. This is a public availability smoke check; I5 does not claim Lighthouse measurements against CloudFront. | CLI authentication expired during an earlier deployment attempt. Scoped ECS task secret injection was added; work resumed after the user authenticated and approved. | I6 deployment complete before I5; current public health and catalog evidence is linked in the README. |
| I5 (complete, merged, and deployed) | Add repeatable API/web coverage gates, high-risk lifecycle regressions, accessibility checks, responsive-browser evidence, and a performance scorecard; publish the frontend refinements | `feat/reliability-scorecard`; implementation commit `41f6b41`; docs commits `38c6bcd`, `5c08a81`; PR #14 merged as `a13eabf` | Codex desktop; Jest, real local PostgreSQL, jest-axe, Chromium agent-browser, Lighthouse 13.5.0, Terraform, AWS CLI; no specialist agents delegated in this iteration | User initiated I5 after confirming the functional happy path was ready, then explicitly approved merge and deployment | Not measured | API 10 suites/88 tests and web 8 suites/76 tests passed. App-wide coverage exceeds 80% in statements, branches, functions, and lines: API 89.68/81.95/94.24/91.01%; web 94.81/91.20/91.22/96.61%. Lighthouse mobile median is 95 performance and 100 accessibility across three local production-build runs. Audit, lint, typecheck, build, Terraform fmt/init/validate, diff check, and public-safe scan passed locally. Hosted `quality`, `database-migrations`, and `infrastructure` passed on PR #14 head `5c08a81`. In `sa-east-1`, the reviewed Terraform plan applied five S3 object actions in the existing private bucket (2 add, 1 update, 2 delete); no ECS, RDS, IAM, Secrets Manager, network, or CloudFront distribution resources changed. Live smoke returned 200 for `/`, the new JS/CSS bundles, API liveness/readiness, catalog, and docs. A post-deploy Terraform plan reports no changes. No real card or charge was used. Full evidence and module-level payment coverage results are in `docs/reliability-scorecard.md`. | Initial Lighthouse run exposed startup layout shift, fixed with a space-reserving catalog skeleton. `payments.service.ts` branches are 74.89% and the payments directory 79.39%, below 80% even though the API aggregate passes; the scorecard discloses the gap. The first deployment plan inherited unsafe defaults (`i1-bootstrap` image and absent sandbox-secret ARN); it was not applied. A second plan preserved current API/migration images and the scoped secret ARN, and changed only S3 assets. | I5 merged and deployed; subsequent I7/I8 decisions and evidence are captured in PRs #16/#17 and `docs/i7-final-acceptance.md`. |
| Post-I8 acceptance-record correction | Align the public README and I7/I8 acceptance report with the merged deployment; add the public Swagger URL and distinguish deployment smoke checks from complete checkout/payment acceptance | `codex/i8-deployment-readme-evidence`; draft PR #18 | Codex desktop, Git, GitHub CLI; no agents delegated for this documentation correction | User supplied the acceptance matrix after the I8 deployment; no new provider payment was requested | Not measured | `git diff --check` passed. The release evidence recorded in the acceptance report confirms the I8 image, ECS revision, HTTP 200 health/readiness/docs checks, and HTTP 200 frontend assets. Documentation-only change; no tests run. | README and audit report still said I8 was undeployed and omitted the full public Swagger URL; corrected those stale claims. Sandbox transaction after I8, physical-device validation, candidate-sharing behavior, and official score remain unverified. | Draft PR #18 prepared for user review; no merge or deployment is part of this correction. |
| I9 (complete, merged, and deployed) | Add typed `Result`/`Err` composition to checkout creation and payment-attempt creation; roll back SQL on expected business failures; translate typed errors at the Nest HTTP edge | `codex/use-case-rop`; feature commit `ff3ff2b`; PR #20 merged as `dc226ce`; implementation `63a1163`; ledger commits `5adf5c0`, `ff3ff2b` | Codex desktop; TypeScript, ESLint, GitHub Actions, Docker ARM64, AWS CLI, Terraform 1.5.7; no specialist agents delegated | User requested ROP, then explicitly authorized merge and deployment; no new payment or lifecycle decision was needed | Not measured | Hosted `quality`, `database-migrations`, and `infrastructure` checks passed. Built and pushed API image `i9-dc226ce` (`linux/arm64`, digest `sha256:964270b8f93e387074ab978db1ba84e7ef4907ee9a00ac2cadd660b8f637f58c`). Terraform API-only plan applied 1 task-definition add, 1 ECS service update, and 1 prior task-definition deregistration; no schema migration. ECS stabilized at 1/1 on task-definition revision 6. Public `/`, liveness, readiness (`database: ok`), catalog, and docs returned HTTP 200. A post-deploy API-only plan reports no changes. No sandbox transaction was submitted. No local tests were run. | The initial full Terraform plan included generated S3 asset drift from the separate, unmerged frontend PR #19; it was not applied. A reviewed targeted plan updated only the API task definition and service. | PR #20 is merged and I9 is deployed. This follow-up records release evidence; sandbox checkout/payment acceptance remains a separate user-operated step. |
| I10 (complete, merged, and deployed) | Correct release and rubric evidence; improve modal keyboard focus, browser boundaries, and static frontend headers; retain API headers separately | `codex/i10-delivery-hardening`; implementation commits `ac3ae06`, `12baf17`; PR #23 merged as `e0f4084`; deployment evidence follow-up PR pending | Codex desktop; Jest, real PostgreSQL, ESLint, TypeScript, Terraform 1.5.7, AWS CLI, and Playwright in Chrome, Firefox, and WebKit; no specialist agents delegated | User requested another criteria-improvement iteration, then explicitly authorized merge, deployment, and root-assisted addition of the missing deployer permissions | Not measured | Hosted `quality`, `database-migrations`, and `infrastructure` checks passed before merge. Web Jest: 8 suites/77 tests; 89.58% statements, 84.06% branches, 86.61% functions, 90.68% lines. API Jest with PostgreSQL migration up/down: 10 suites/88 tests; 90.00% statements, 82.14% branches, 93.41% functions, 91.20% lines. Local lint, typecheck, production web build, Terraform format/validate, and diff check passed. Initial apply published the three new S3 assets, updated `index.html`, and removed the two retired hashed assets; a missing CloudFront permission stopped the policy creation. After adding a project-bucket-scoped S3 read policy and CloudFront policy create/read plus distribution-scoped update permissions to the deployer, the refreshed plan was 1 add, 1 change, 0 destroy: create the static response-header policy and update the existing distribution. The API stayed on image `i9-dc226ce` and the migration task image stayed `i8-2bc9e7f`; ECS, RDS, network, secrets, and public hostname were unchanged. Terraform apply completed, CloudFront distribution `E32W86Q7LZSYU1` reports `Deployed`, and a post-apply plan reports no changes. Public app, readiness, and Swagger returned HTTP 200. The live static response includes CSP, HSTS, `nosniff`, `DENY`, and `no-referrer`. Production Playwright checks passed in Chrome, Firefox, and WebKit at 375 × 667, 667 × 375, and 1280 × 800 CSS pixels, covering catalog, image loading, modal boundaries, focus/trap/restoration, progress, and console errors. No sandbox payment submitted. | WebKit pointer activation did not focus the product button; explicit trigger capture and restoration fixed the cross-browser failure. The first deployer-only plan lacked `s3:GetBucketAcl`, `ec2:GetManagedPrefixListEntries`, and further bucket-configuration reads; a project-bucket-scoped read policy resolved Terraform refresh. The first apply then lacked CloudFront policy creation/update rights; a policy-creation grant and distribution-scoped update grant resolved the release. The temporary root CLI credential cache was cleared after deployment. The payments module still has 79.40% branch coverage and physical-device behavior remains unmeasured. | I10 is merged and deployed. Follow-up PR will record the release evidence in README and traceability docs. The public app/API remain on the existing CloudFront/Fargate setup; no new compute or database resource was added. |

| I12 (implementation complete; awaiting review) | Move checkout, payment, catalog, guest-session, PII-retention, and readiness persistence behind application-owned ports; bind PostgreSQL adapters in Nest modules; prevent direct database imports from app services/controllers; document transaction consistency vs domain idempotency | `codex/i12-hexagonal-architecture`; commit `927f352`; draft PR #26 | Codex desktop; TypeScript, ESLint, Jest, PostgreSQL 17 via Colima, Git; no specialist agents delegated | User requested an iteration to make the API genuinely Hexagonal; no lifecycle or provider decision changed | Not measured | Local lint, typecheck, build, and diff checks passed. API Jest with isolated PostgreSQL migrations up/down: 10 suites / 92 tests passed; 92.94% statements, 84.60% branches, 96.05% functions, 94.79% lines. Hosted `quality`, `database-migrations`, and `infrastructure` checks passed on PR #26. ESLint blocks direct `pg` and database-module imports in application services/controllers. No provider request, sandbox charge, Terraform apply, merge, or deployment was performed. Architecture diagram and boundaries are in `docs/hexagonal-architecture.md`. | First test run exposed a checkout transition regression on provider-detail mismatch (`UNKNOWN_OUTCOME`); it was fixed and covered by the passing rerun. Two tests were updated to use the new persistence-port shapes. | Draft PR is ready for user review. Merge and deployment remain separate gates; the current AWS release is unchanged. |

## Summary to complete at the end

- Iterations to the first complete acceptance pass:
- Additional correction iterations to stable pass:
- Human interventions and decisions:
- Time and coordination/rework evidence:
- Defects escaped to later iterations:
- Rubric points verified, with evidence:
- Deployment and publication gates actually completed:
